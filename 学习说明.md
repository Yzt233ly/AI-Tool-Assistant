# AI Tool Assistant 学习说明

这是一个用 **Python + LangChain + LLM + Tool Calling** 实现的简单 AI 工具助手。

它能根据你的问题，**自主判断**是否需要调用工具、调用哪个工具、给工具传什么参数，最后结合工具的执行结果生成回答。核心就一句话：

> 让 LLM 不再只会「说」，还能「做」——通过调用真实的 Python 函数去查汇率、查天气、查名言。

---

## 一、怎么运行

### 1. 配置 API Key

项目使用 DeepSeek 的大模型，需要你的 API Key。

```text
1. 把 .env.example 复制一份，改名为 .env
2. 打开 .env，把 DEEPSEEK_API_KEY= 后面的内容换成你自己的 Key
```

> 为什么要用 `.env`？因为 API Key 相当于密码，写死在代码里一旦推到 GitHub 就会泄露。
> `.env` 已经被 `.gitignore` 忽略，不会被提交。

### 2. 安装依赖

```bash
.venv\Scripts\python.exe -m pip install -r requirements.txt
```

### 3. 启动服务

```bash
.venv\Scripts\python.exe -m uvicorn main:app --reload
```

看到类似 `Uvicorn running on http://127.0.0.1:8000` 就说明启动成功了。

### 4. 打开页面

浏览器访问 **http://127.0.0.1:8000**，输入问题试试：

- 「100 美元等于多少人民币？」
- 「北京现在天气怎么样？」
- 「给我来一条名言」

---

## 二、技术栈清单

| 技术/库 | 是什么 | 在项目里干什么 |
|---------|--------|----------------|
| **Python 3.11** | 编程语言 | 整个项目的骨架，所有代码都用它写 |
| **LangChain** | 一套让 LLM 更好用的工具库 | 提供 `@tool` 装饰器、消息类型、`bind_tools` 等，帮我们把「工具」和「LLM」串起来 |
| **LLM（大语言模型）** | 能理解和生成文本的 AI 模型 | 项目的「大脑」，负责判断要不要用工具、用哪个、怎么组织最终回答 |
| **Tool Calling** | LLM 调用外部工具的一种能力 | 项目的核心机制，让 LLM 从「只能说话」变成「能执行函数」 |
| **langchain-openai** | LangChain 的 OpenAI 兼容接口 | 用来连 DeepSeek（因为 DeepSeek 提供了 OpenAI 兼容协议） |
| **Pydantic** | 数据校验库 | 校验前端传来的请求格式；也是工具参数类型的底层 |
| **FastAPI** | Python 的 Web 框架 | 提供 HTTP 接口和页面，让浏览器能和我们对话 |
| **uvicorn** | Web 服务器 | 真正把 FastAPI 应用跑起来、接收请求 |
| **httpx** | 发 HTTP 请求的库 | 工具里用它去访问汇率、天气、名言这些外部 API |
| **python-dotenv** | 读取 `.env` 文件 | 把 API Key 从文件加载到环境变量里 |

### 几个关键概念用大白话解释

**LLM（大语言模型）**：一个「预测下一个词」的超大神经网络。你给它一段文字，它接着往下生成。它很会聊天，但它自己**不会联网、不会算数、不会查实时数据**——它只能基于训练时记住的知识回答。

**Tool Calling（工具调用）**：解决「LLM 只会说、不会做」的办法。思路是：让 LLM **不要自己硬算/硬猜**，而是输出一个「我要调用某某工具，参数是某某」的指令，由我们的 Python 代码真正去执行，再把结果喂回给 LLM。这样 LLM 就「借」到了外部能力。

**Pydantic**：用来描述「数据长什么样」并自动校验。比如我们定义 `message: str`，如果前端传来一个数字，FastAPI/Pydantic 会自动拒绝并报错，不用我们手写判断。

---

## 三、Tool Calling 核心概念（你关心的 8 个问题）

### 1. Tool Calling 是什么？

简单说：**让 LLM 在回答之前，先告诉你要调用哪个工具、参数是什么，然后由你的代码真正执行这个工具，再把结果交给 LLM 生成最终答案。**

它不是让 LLM 自己去联网或运行代码，而是让 LLM「点菜」：它说「我要查北京的天气」，我们后厨（Python 函数）去做菜，做完把菜端给 LLM，LLM 再把它包装成漂亮的话说给用户。

### 2. Tool Calling 为什么存在？

因为 LLM 有天然的短板：

- **不知道实时信息**：训练数据有截止时间，问「今天汇率多少」它答不准。
- **不会精确计算**：复杂数学它容易算错。
- **无法访问外部系统**：不能查数据库、不能调 API。

Tool Calling 就是给 LLM「外接手臂」的机制——让它遇到自己搞不定的事时，交给真正的函数去做。

### 3. LLM 怎么选择工具？

关键在 `bind_tools`。它会把每个工具的「说明书」（名字、描述、参数说明）打包成 JSON，随你的问题一起发给 LLM。

LLM 读说明书后，根据语义判断：这个问题该不该用工具、用哪个最合适。比如：

- 「100 美元等于多少人民币」→ 涉及汇率 → 选 `get_exchange_rate`
- 「北京天气怎么样」→ 涉及天气 → 选 `get_weather`
- 「你好啊」→ 不需要工具 → 直接回答

### 4. Tool 参数怎么产生？

LLM 选好工具后，会根据工具说明书里的参数定义，**自己生成**参数值。

比如它选了 `get_exchange_rate`，说明书要求 `from_currency` 和 `to_currency` 两个字符串，它就会从你的问题里提取：

```json
{"from_currency": "USD", "to_currency": "CNY"}
```

这个参数不是我们写的，是 LLM「读懂了问题」之后填出来的。这正是 Tool Calling 神奇的地方。

### 5. Python 怎么真正执行 Tool？

LLM 返回的其实只是一串**字符串**（工具名 + 参数）。真正执行的是我们的 Python 代码：

```python
tool = TOOL_MAP[tool_name]      # 用名字找到工具对象
result = tool.invoke(tool_args) # 真正调用这个 Python 函数
```

`tool.invoke(...)` 会去执行函数体里的代码——发 HTTP 请求、解析 JSON、返回结果。**LLM 从头到尾没有自己联网，联网的是这段 Python。**

### 6. Tool 执行结果怎么重新返回 LLM？

执行结果要包装成 `ToolMessage`，并带上 `tool_call_id`：

```python
ToolMessage(content=result_text, tool_call_id=tool_call["id"])
```

这个 `tool_call_id` 就像「回执编号」。LLM 之前发出调用时带了一个编号，我们把结果连同一个编号还给它，它才知道「哦，这是我刚才要查的那笔汇率」。

然后把 `ToolMessage` 加进消息列表，**再次调用 LLM**，它就会结合工具结果，生成最终的、自然的回答。

### 7. LangChain 在整个过程中做了什么？

LangChain 不是必须的（你完全可以手写整个流程），但它帮我们省掉了很多繁琐的「胶水代码」：

| LangChain 提供的东西 | 作用 |
|----------------------|------|
| `@tool` 装饰器 | 自动把普通函数变成工具，自动生成工具的「说明书」（名字/描述/参数） |
| `bind_tools` | 自动把工具说明书转成 LLM 能懂的格式，随请求一起发出去 |
| 消息类型（`HumanMessage`/`ToolMessage`/`AIMessage`） | 统一管理对话历史，不用自己拼字符串 |
| `tool.invoke()` | 统一调用方式，负责参数校验、类型转换 |

一句话：LangChain 把「和 LLM 打交道」的格式细节标准化了。

### 8. 整个项目的代码是怎么运行起来的？

见下一节「代码运行流程」。

---

## 四、代码运行流程（从头到尾）

```
浏览器（前端）
    │  1. 用户输入问题，JS 通过 fetch 发 POST /chat
    ▼
main.py 的 chat() 函数
    │  2. 把问题包成 HumanMessage
    ▼
config.py 里的 llm_with_tools（已 bind_tools 的 LLM）
    │  3. 第一次调用 LLM，LLM 决定是否调工具
    ▼
如果 LLM 返回 tool_calls（要调工具）
    │  4. 遍历每个 tool_call，从 TOOL_MAP 找到工具
    │  5. tool.invoke(参数) 真正执行函数（发 HTTP 请求）
    │  6. 把结果包成 ToolMessage 回传
    │  7. 再次调用 LLM，让它结合结果生成最终回答
    ▼
返回 {"reply": 最终回答, "tools": 调用过的工具}
    │  8. 前端展示回答 + 「调用了哪些工具」的提示
    ▼
浏览器显示结果
```

**对应到文件：**

- [tools.py](tools.py) —— 定义「工具」（三个能发 HTTP 请求的函数）
- [config.py](config.py) —— 加载配置、创建 LLM、`bind_tools` 绑定工具
- [main.py](main.py) —— Web 接口 + Tool Calling 的完整循环
- [static/index.html](static/index.html) —— 前端页面

---

## 五、常见难点与解决

### 难点 1：忘了配置 `.env`，一启动就报错

**现象**：运行 `uvicorn main:app` 时报错 `ValueError: 没有找到 DEEPSEEK_API_KEY...`

**原因**：`config.py` 在启动时就会去读 Key，没有 `.env`（或没填 Key）就直接报错。

**解决**：复制 `.env.example` 为 `.env`，填上真实 Key。注意文件名必须是 `.env`（Windows 下复制后可能变成 `.env.txt`，要在资源管理器「显示扩展名」确认）。

### 难点 2：DeepSeek 调用报 401 / 认证失败

**现象**：发消息后报 `401 Unauthorized` 或 `AuthenticationError`。

**原因**：Key 填错了、Key 前后有多余空格、或者 Key 没充值/余额不足。

**解决**：检查 `.env` 里 Key 是否完整、无空格；去 DeepSeek 后台确认 Key 有效且有余额。也可以在 [config.py](config.py) 里临时 `print` 一下 Key 长度确认读到了。

### 难点 3：工具能跑通，但 LLM 不调用工具 / 答非所问

**现象**：问「北京天气」，LLM 直接说「我不知道」，而不是调用 `get_weather`。

**原因**：通常是工具的描述（docstring）写得不清楚，LLM 没看懂这个工具是干嘛的；或某个模型对工具调用支持不佳。

**解决**：把工具的 docstring 写清楚「干什么 + 参数含义 + 返回什么」；确认用的是 `deepseek-chat`（它支持工具调用）；可以把 `temperature` 调低（如 0.3）让选择更稳定。

### 难点 4：外部 API 报错（证书过期 / 域名迁移 / 网络不通）

**现象**：工具执行时报 `CERTIFICATE_VERIFY_FAILED` 或 `301 Moved Permanently` 之类的错。

**原因**：免费第三方 API 有时会换域名、证书过期，或者你所在网络访问不了国外接口。

**解决**：
- 域名迁移 → 换成新地址（本项目就遇到了 Frankfurter 从 `.app` 迁到 `.dev`）。
- 证书过期 → 换一个更稳定的同类 API（本项目把名言接口从 Quotable 换成了 DummyJSON）。
- 网络不通 → 换成国内可访问的接口，或加代理。

> 注意：不要用「关闭 SSL 校验」来绕过证书错误，那是危险的坏习惯，会降低安全性。

### 难点 5：前端页面空白 / 请求 404

**现象**：浏览器打开 8000 端口是空白，或控制台报 404。

**原因**：`index.html` 路径不对；或者你在 FastAPI 里改了路由但没同步。

**解决**：确认 [main.py](main.py) 里 `FileResponse` 指向的 `static/index.html` 确实存在；确认启动命令是 `uvicorn main:app`（`main` 是文件名，`app` 是里面的 FastAPI 实例名）。
